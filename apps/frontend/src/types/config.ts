export interface NavConfig {
  logoAlt: string;
  logoRoute: string;
  logoSrc: string;
}

export interface HeroConfig {
  heading: string;
  subheading: string;
  paragraph: string;
}

export interface ProfileConfig {
  avatar: string;
  name: string;
  title: string;
  funFacts: string[];
}
